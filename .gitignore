# Local Claude Code tool permissions — machine-specific, not part of the app.
.claude/settings.local.json

# macOS
.DS_Store

# Python bytecode from the pack builder
__pycache__/

# Editors
.vscode/
.idea/
*.swp

# Scratch harness used while building the map-to-block fit. Holds an older
# copy of geo/infer.js and a superseded georef.js, so it must not ship
# alongside the real modules.
lab/
